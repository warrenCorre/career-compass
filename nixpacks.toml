[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn --timeout 120 --workers 1 --threads 4 --bind 0.0.0.0:$PORT app:app"