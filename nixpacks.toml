[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --timeout 120 --workers 1 --threads 4 --bind 0.0.0.0:$PORT app:app"